export const first_number = 0;

// Reminder to keep the current_year value updated every year

import news from './news.js';

const dates = Object.keys(news);

export const current_number = dates.length - 1;
